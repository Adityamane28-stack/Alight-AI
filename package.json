{
  "name": "alight-ai-assistant",
  "version": "1.0.0",
  "description": "Alight - Autonomous Multi-Provider AI Assistant",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build:client": "npm run build --prefix client",
    "build:server": "npm run build --prefix server",
    "build": "npm run build --prefix client && npm run db:generate --prefix server && npm run db:push --prefix server && npm run build --prefix server",
    "start": "node server/dist/index.js",
    "postinstall": "npm run db:generate --prefix server",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  }
}

